```
//全局正则表达式打印
grep
        -i  忽略大小写
        -v  显示不匹配的行
        -n  显示匹配行的行号
        -l  只列出包含匹配字符串的文件名
        -c  仅显示匹配行的计数
        -E  支持扩展正则表达式
        -W  匹配整个单词
        -A  显示匹配行 及其后的行数
        -B  显示匹配行 及其前的行数
        -C  显示匹配行 及其前后的行数
        -r  递归搜索目录及其子目录的文件
```